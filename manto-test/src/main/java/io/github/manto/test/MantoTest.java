package io.github.manto.test;

/**
 * Placeholder for {@code manto-test} testing utilities.
 *
 * <p>v1.0 ships this module as an empty placeholder to reserve the
 * {@code io.github.kaiser-haque:manto-test} coordinate for future testing
 * helpers (see {@code docs/ARCHITECTURE.md}). The class ensures the module
 * produces a non-empty source and Javadoc JAR as required by Maven Central
 * for {@code jar} packaging.
 */
public final class MantoTest {

    private MantoTest() {}
}
